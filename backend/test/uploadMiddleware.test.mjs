import { jest } from '@jest/globals';
import * as uploadMiddleware from '../middleware/uploadMiddleware.js';
describe('uploadMiddleware', () => {
  it('should handle file uploads', () => {});
  it('should handle app logo uploads', () => {});
}); 